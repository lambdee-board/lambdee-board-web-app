import { createTheme } from '@mui/material'

const lambdeeTheme = createTheme({
  palette: {
    primary: {
      main: '#1082F3'
    },
    secondary: {
      main: '#010A13'
    }
  }
})

export default lambdeeTheme
