# frozen_string_literal: true

# Controller which serves the frontend React app.
class FrontendController < ::ApplicationController
  # Main action that serves the
  # frontend React app
  def app
    nil
  end
end
