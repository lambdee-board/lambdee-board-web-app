#!/usr/bin/env bash

script_dir=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
. $script_dir/shell_helper "$@"

title "Forcing resetting the database"

DISABLE_DATABASE_ENVIRONMENT_CHECK=1 $script_dir/rails db:reset
