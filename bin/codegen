#!/usr/bin/env bash
script_dir=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
. $script_dir/shell_helper "$@"

title "Running Playwright Codegen on localhost:3000"

npx playwright codegen localhost:3000
